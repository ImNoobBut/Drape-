{
  "name": "drape-tryon",
  "version": "1.0.1",
  "private": true,
  "description": "Drape — live virtual try-on Chrome extension powered by Decart Lucy V-TON",
  "workspaces": [
    "extension",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev -w extension",
    "build": "npm run build -w extension",
    "server": "npm run dev -w server",
    "start": "npm run start -w server"
  },
  "engines": {
    "node": ">=18"
  }
}
